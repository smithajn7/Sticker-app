
export interface StyleOption {
  name: string;
  id: string;
  prompt: string;
}

export interface ExpressionOption {
  name: string;
  id: string;
  prompt: string;
}

export interface GeneratedSticker {
  id: string;
  imageUrl: string;
  prompt: string;
}
